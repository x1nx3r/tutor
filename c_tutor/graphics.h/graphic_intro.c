#include <stdio.h>
#include <math.h>

double N;
double A;
double W=2;
int main(){
    scanf("%d",&N);
    double X=sqrt(N);
    printf("squrt = %lf",N);
    /*
    A=sqrt(N);
    if (A==W){
        printf("Ya");
    } else {
        printf("Tidak");
    }
    */
    return 0;
}