import math

def crt_array(maxlen):
    counter=0
    arr=[]
    while counter<maxlen :
        counter+=1
        arr.append(counter)
    return arr


def if_prime(ifprime):
    isPrime=1
        

print(if_prime(9))